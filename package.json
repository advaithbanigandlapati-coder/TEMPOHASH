{
  "name": "tempo",
  "version": "1.0.0",
  "private": true,
  "description": "Private collaboration platform — channels, calendar, notes, video, canvas",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "engines": {
    "node": ">=18"
  }
}
